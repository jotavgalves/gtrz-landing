import { useEffect,useState } from 'react';import { api } from '../../lib/api';import { Panel } from '../../components/StateViews';
export function People(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{api<any>('/api/admin/freelancers').then(r=>setItems(r.results||[])).catch(()=>{});},[]);return <Panel title="Pessoas" eyebrow="Equipe e freelancers"><div className="subnav"><button>Equipe</button><button className="active">Freelancers</button></div><div className="list">{items.map(x=><article key={x.id}><div><strong>{x.name}</strong><span>{x.city}{x.state?' · '+x.state:''} · {x.status}</span></div><button>Ver perfil</button></article>)}</div></Panel>}
