import { useEffect,useState } from 'react';import { api } from '../../lib/api';import { Panel } from '../../components/StateViews';
export function Analytics(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api<any>('/api/admin/analytics/overview').then(r=>setRows(r.results||[])).catch(()=>{});},[]);return <Panel title="Analytics" eyebrow="Aquisição e comportamento"><div className="analytics-intro"><div><strong>Origem</strong><span>UTM + links /r/:slug + referrer</span></div><div><strong>Comportamento</strong><span>page, seção, CTA, popup, scroll, ingresso</span></div><div><strong>Conversões</strong><span>ingresso, Instagram, freelancer, parceria</span></div></div><div className="table"><div className="thead"><span>Dia</span><span>Métrica</span><span>Dimensão</span><span>Valor</span></div>{rows.slice(-100).map((r,i)=><div key={i}><span>{r.day}</span><span>{r.metric}</span><span>{r.dimension_key||'—'}</span><span>{r.value}</span></div>)}</div></Panel>}
