import { useEffect,useState } from 'react';import { api } from '../../lib/api';import { Panel } from '../../components/StateViews';
export function Commercial(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{api<any>('/api/admin/partnerships').then(r=>setItems(r.results||[])).catch(()=>{});},[]);return <Panel title="Parcerias" eyebrow="Comercial"><div className="pipeline"><span>Novo</span><span>Em análise</span><span>Contato</span><span>Negociação</span><span>Fechado</span></div><div className="list">{items.map(x=><article key={x.id}><div><strong>{x.company_name||x.contact_name}</strong><span>{x.partnership_type} · {x.status}</span></div><button>Abrir</button></article>)}</div></Panel>}
